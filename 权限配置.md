# LightBot 权限与数据隔离配置指南

> 面向：企业集成开发 / 平台建设者  
> 用途：说明「能力边界」与「行级隔离」如何配置，以及 `callerContext` 在各链路的生效方式  
> 前置：已执行 `sql/2026-08-05-001.sql`（`chat_session.caller_context`、`ask_dataset.tenant_dimensions`）  
> 在线 API 说明：控制台「开放文档」或 [lightbot-ui/src/constants/apiKeyDocs.js](lightbot-ui/src/constants/apiKeyDocs.js)

---

## 1. 两层权限，不要混为一谈

| 层级 | 控制什么 | 配置入口 | 谁负责 |
| --- | --- | --- | --- |
| **能力边界** | 能用哪些 Agent / 工具 / 业务页 / 问数模型 | API Key、Agent 绑定 | LightBot 平台 |
| **行级 / 租户隔离** | 同一能力下只能看本地区/本企业数据 | 请求 `callerContext` + 下游配置 | 上游业务断言身份；平台透传并在问数侧强制过滤 |

```text
上游业务系统 ──Bearer API Key──► LightBot
                                 │
                                 ├─ 能力：Key 白名单 ∩ Agent 绑定
                                 └─ 身份：callerContext → Tool / MCP / 问数
```

原则：

- **信任边界在 API Key**：智元不校验上游 SSO；调用方用合法 Key 声明终端用户身份。
- **`callerContext` 默认不进系统提示词**：避免敏感身份进模型；要给模型看请用 `bizParams`。
- **`bizParams` 只做提示词变量**，不参与 Tool/MCP/问数隔离。

---

## 2. 能力边界怎么配

### 2.1 企业 API Key

控制台 → API Key：

1. 创建 Key，权限选 `chat`（仅对话）或 `full`（对话 + 开放数据接口）。
2. **绑定可调用的已发布 Agent**（开放 API 不能调草稿）。
3. 按需配置：限流、每日 Token 配额、业务页白名单等。

调用时：

```http
Authorization: Bearer lbkey_xxxx
Content-Type: application/json
```

### 2.2 Agent 侧白名单

| 能力 | Agent 配置位置 | 说明 |
| --- | --- | --- |
| 工具 / MCP / Skill | Agent 详情对应 Tab | 未绑定则运行时不可用 |
| 业务办理页 | 「业务页组件」Tab | 与 API Key 业务页白名单取交集；未绑定不注入 `present_business_page` |
| 问数 | 「可问数据」勾选数据分类/模型 | 未勾选则问数工具不可查 |

能力白名单解决的是「能不能用这个工具」，**不能**单独保证「只能查本企业数据」。

---

## 3.0 控制台「模拟角色」（实验室对话）

建设者可在对话顶栏切换多套模拟 `callerContext`，用于联调问数行业/企业隔离，无需每次走开放 API。

| 项 | 说明 |
| --- | --- |
| 入口 | 实验室对话 → 顶栏右侧「模拟角色」 |
| 字段 | `externalUserId` / `regionId` / `enterpriseId` / `profile`（均可模拟） |
| 未选角色 | `externalUserId` 回落 `debug_user_{建设者ID}` |
| 切换规则 | 会话已有消息时**禁止**同会话改身份，确认后新建对话 |
| 存储（一期） | 浏览器 `localStorage`，key=`lightbot.simCallerRoles.v1.{userId}`，JSON `version/activeRoleId/roles[]`（便于后续迁库） |
| 服务端 | 控制台接受请求中的模拟身份，并写入 platform 会话 `caller_context`；续聊从会话回填 |

---

## 3. 调用方身份 `callerContext`

### 3.1 请求字段

对话接口（`POST /api/chat`、`POST /api/chat/stream`）Body：

```json
{
  "agentId": "2081297581920317442",
  "message": "查一下本月订单",
  "sessionId": null,
  "callerContext": {
    "externalUserId": "biz_user_10086",
    "regionId": "330100",
    "enterpriseId": "ent_001",
    "profile": {
      "name": "张三",
      "roles": ["admin"],
      "deptId": "d_12"
    }
  },
  "bizParams": {
    "userName": "张三"
  }
}
```

| 字段 | 用途 | 是否进模型 |
| --- | --- | --- |
| `externalUserId` | 终端用户标签；长期记忆命名空间；会话绑定 | 否（除非你再写进 bizParams/提示词） |
| `regionId` / `enterpriseId` | 租户隔离主键；问数强制过滤；Tool/MCP 可读 | 否 |
| `profile` | 完整身份扩展，供 Tool/MCP 读取 | **否**（默认） |
| 顶层 `externalUserId` | 兼容字段，等价于 `callerContext.externalUserId` | 否 |
| `bizParams` | 替换系统提示词 `{{变量}}` | **是**（进提示词） |

隔离主键格式：最长 128，仅允许字母、数字与 `. _ - : @ /`。

### 3.2 会话绑定规则

1. **首轮**带上 `callerContext` → 写入 `chat_session`（`external_user_id` + `caller_context` JSONB）。
2. **续聊**可省略 `callerContext`，服务端从会话回填，Tool/MCP/问数仍能拿到完整身份。
3. 续聊若传入与已绑定不一致的 `externalUserId` / `regionId` / `enterpriseId` → **拒绝**（错误码 `94012`）。
4. `profile` 允许后续请求更新并写回会话；隔离主键不可换。

### 3.3 想让模型「认识用户」

`callerContext` **不会**自动让模型说出「你是张三」。需要：

1. 在 Agent 系统提示中声明变量，例如：`当前用户：{{userName}}，地区：{{regionId}}`；
2. 请求里用 `bizParams` 传值：

```json
{
  "bizParams": {
    "userName": "张三",
    "regionId": "330100"
  }
}
```

长期记忆是另一条链路：启用记忆策略后，按 `apiKeyId + externalUserId` 存取；不会自动把本次 `profile` 写成记忆。

---

## 4. 问数行级隔离（平台强制）

### 4.1 配置 `tenantDimensions`

在问数数据集上声明：**callerContext 键 → { 表字段, 匹配策略 }**。仅支持 `regionId` / `enterpriseId`（已移除终端用户维度）。

控制台：**数据模型卡片 → 问数增强 → 数据隔离**（也可走增强 API）。

```json
{
  "tenantDimensions": {
    "regionId": { "field": "regionId", "match": "subtree" },
    "enterpriseId": { "field": "enterpriseId", "match": "eq" }
  }
}
```

兼容旧格式：`"regionId": "regionId"` 等价于 `{ "field": "regionId", "match": "eq" }`。

**运行时角色（互斥，非 AND）：**

| 调用方 | 判定 | 生效过滤 | 忽略 |
| --- | --- | --- | --- |
| 企业用户 | 传入了 `enterpriseId` | 仅企业维度（精确匹配） | 地区维度（即使也传了 `regionId`） |
| 行业用户 | 未传 `enterpriseId` | 仅地区维度（配了则 `regionId` 必填） | 企业维度 |

| match | 含义 | 适用 |
| --- | --- | --- |
| `eq` | 精确相等（默认） | 企业 ID |
| `subtree` | **本级及下级**（查平台地区库展开后 `IN`） | **地区推荐** |
| `prefix` | 字符串前缀（`starts_with`） | 自定义路径编码 |
| `in` | 多值 IN；逗号分隔或 JSON 数组 | 上游自行展开列表 |

### 4.1.1 地区库（国标省市区）

- 表：`region`（见 `sql/2026-08-05-002.sql`）
- 种子：classpath `region/china-pca.json`（省/市/区县，约 3400 条）
- 控制台：**数据中心 → 地区库**（查看树、搜索、导入 / 重新导入）
- 启动时空库自动 `seedIfEmpty`
- `code` 统一为国标 **6 位**：省 `510000`、市 `510100`、区 `510104`（东莞/中山镇街等可为 9 位）
- `callerContext.regionId` 兼容短码：`51` / `5101` 会解析为 `510000` / `510100`

**地区「上级看下级」推荐（subtree）：**

1. 业务表字段存区划 `code`（与地区库一致，推荐 6 位，如车辆表 `regionId`）。
2. **数据中心 → 地区库** 确认已导入。
3. 问数增强 → 数据隔离：地区 → 映射该字段，勾选 **包含下级**（默认勾选）。
4. 成都市用户：`"regionId": "510100"`（或短码 `5101`）→ 引擎展开本市及下属区县编码后 `IN` 过滤。
5. 某区用户：`"regionId": "510104"` → 仅本区及更下级（若有）。

说明：

- 左侧键仅支持：`regionId` / `enterpriseId`。
- `field` 必须是数据模型已有字段 key。
- 两边都可配置；运行时按角色只生效一维。行业缺 `regionId`（且配了地区）→ `110026`；`subtree` 且地区库无此编码 → 查询失败。
- `externalUserId` 仍可用于长期记忆 / 会话绑定，但不参与问数行级隔离。

### 4.2 配置步骤（建设者）

1. 执行 `sql/2026-08-05-002.sql`；**数据中心 → 地区库** 确认有数据（或点导入）。
2. 数据模型 **表单结构**：添加 `regionId` / `enterpriseId` 等字段，写入样例（区划用库内 `code`）。
3. 模型卡片 → **问数增强** → 启用。
4. **数据隔离**：地区选字段并勾选 **包含下级**；企业选字段（精确匹配）；保存。
5. Agent：「可问数据」勾选该模型。
6. 行业调用只传 `regionId`；企业调用传 `enterpriseId`（可不传地区）。
7. 验收：行业成都身份能看高新区行且不按企业收窄；企业身份只看本企业全量、不按地区收窄；行业不传 `regionId` 应失败。

### 4.3 与默认过滤的关系

- 数据集 `defaultFilters`、指标固化过滤仍然生效。
- 与租户维度**同名字段**会被租户强制条件覆盖，避免与强制隔离冲突。

更多问数产品/技术背景见 [ASK_DATA.md](ASK_DATA.md)。

---

## 5. MCP 如何用身份做隔离

### 5.1 平台侧（无需改 MCP 连接 Header）

工具执行时，Spring AI 将可序列化 ToolContext 写入 MCP `CallToolRequest` 的 **`_meta`**，其中包含：

```json
{
  "agentId": "...",
  "sessionId": "...",
  "apiKeyId": "...",
  "externalUserId": "biz_user_10086",
  "regionId": "330100",
  "enterpriseId": "ent_001",
  "callerContext": {
    "externalUserId": "biz_user_10086",
    "regionId": "330100",
    "enterpriseId": "ent_001",
    "profile": { "name": "张三" }
  }
}
```

**不要**依赖「每个用户改 MCP Server 的 HTTP Header」：MCP Client 按 `serverId` 缓存，连接级 Header 会串扰。

### 5.2 MCP Server 侧（业务开发）

在工具实现里读取 `_meta.callerContext`（或扁平 `regionId` / `enterpriseId`），自行拼查询条件或调业务 API：

```text
伪代码：
  ctx = request.meta.callerContext
  if ctx.enterpriseId is blank → return error
  query = businessDb.query(..., enterpriseId=ctx.enterpriseId, regionId=ctx.regionId)
```

Agent 需绑定该 MCP Server；未绑定则模型调不到工具。

---

## 6. HTTP API 工具如何注入身份

工具实体 `config`（JSONB）支持模板（从 ToolContext / callerContext 渲染）：

```json
{
  "method": "GET",
  "contextHeaders": {
    "X-Region-Id": "{{callerContext.regionId}}",
    "X-Enterprise-Id": "{{callerContext.enterpriseId}}",
    "X-External-User-Id": "{{callerContext.externalUserId}}"
  },
  "contextQuery": {
    "regionId": "{{regionId}}",
    "enterpriseId": "{{enterpriseId}}"
  }
}
```

可用占位符示例：

- `{{callerContext.regionId}}` / `{{regionId}}`
- `{{callerContext.enterpriseId}}` / `{{enterpriseId}}`
- `{{callerContext.externalUserId}}` / `{{externalUserId}}`
- `{{callerContext.profile.name}}` / `{{profile.name}}`
- `{{apiKeyId}}` / `{{sessionId}}`

业务 HTTP 接口收到这些 Header/Query 后自行鉴权与过滤。空值不会发出去（渲染结果为空则跳过该项）。

---

## 7. 内置 Tool / SubAgent

执行时 ToolContext 始终带有（与 MCP 同源可序列化字段）：

- `callerContext`（Map）
- 扁平：`externalUserId`、`regionId`、`enterpriseId`
- `apiKeyId`、`agentId`、`sessionId`、`userId` 等

自研内置工具从 `ToolContext.getContext()` 读取即可。SubAgent 委派会携带同一运行时上下文（以当前实现的 RuntimeContext 传递为准）。

---

## 8. 业务办理页

- **能力**：Agent「业务页组件」绑定 ∩ API Key 业务页白名单。
- **行级（身份透传）**：`present_business_page` 从 ToolContext 写入权威 `callerContext`（与业务 `props` 分离，模型不可伪造）。前端 `init` / `postMessage` 带上该快照；**身份不进 URL**。
  - **内嵌 pageHtml**：桥接默认证出站 Header（可关）：
    - `X-Zhiyuan-External-User-Id`
    - `X-Zhiyuan-Region-Id`
    - `X-Zhiyuan-Enterprise-Id`
  - 页面也可：`LightBot.getCallerContext()` / `LightBot.getIdentityHeaders()`
  - **外链 pageUrl**：仅 `postMessage` 下发身份，无法包装对方 `fetch`；需要平台强制 Header 请用内嵌 HTML
  - 注册页无需配置：平台默认开启出站 Header 与 profile 透传（高级映射仍可由页面 `options`/`default_options` 覆盖，一般不必）
  - 业务鉴权应以隔离三键 Header 为准，勿只信 Body / `profile`
- 办理结果回灌对话：走既有 submit/cancel 协议即可。

---

## 9. 配置清单（按角色）

### 集成开发（上游业务）

- [ ] 使用已发布 Agent + 合法 API Key
- [ ] 首轮对话传入完整 `callerContext`（至少隔离主键）
- [ ] 续聊回传 `sessionId`；勿中途更换隔离主键
- [ ] 需要模型「认识用户」时另配 `bizParams` + 提示词变量
- [ ] MCP / 业务 HTTP 在服务端按 `_meta` 或 Header 做过滤（勿只靠模型自觉）

### 平台建设者（LightBot 控制台）

- [ ] 执行 SQL：`sql/2026-08-05-001.sql`；业务页身份透传：`sql/2026-08-06-001.sql`
- [ ] API Key 绑定 Agent、配额与业务页白名单
- [ ] Agent 绑定工具 / MCP / 可问数据 / 业务页
- [ ] 执行 `sql/2026-08-05-002.sql`，地区库已导入
- [ ] 需问数行级隔离：问数增强「数据隔离」配置地区 + 企业；地区勾选 **包含下级**
- [ ] 需透传身份的 API 工具配置 `contextHeaders` / `contextQuery`
- [ ] 回归：行业只传 `regionId`；企业只传 / 兼传 `enterpriseId`（应忽略地区）

---

## 10. 相关错误码

| 码 | 含义 |
| --- | --- |
| 94009 | `externalUserId` 格式非法（或与权限不足码历史复用，以实际 message 为准） |
| 94010 | 会话已绑定其他 `externalUserId` |
| 94011 | `callerContext` 字段非法 |
| 94012 | 会话已绑定其他调用方身份（region/enterprise/user 冲突） |
| 110026 | 问数要求租户身份但请求未提供（`ASK_DATA_TENANT_REQUIRED`） |

---

## 11. 相关代码与文档

| 位置 | 说明 |
| --- | --- |
| [sql/2026-08-05-001.sql](sql/2026-08-05-001.sql) | 会话身份 / 问数租户维度表结构 |
| `com.lightbot.dto.CallerContext` | 身份模型 |
| `InitMiddleware` | 规范化、会话绑定与回填 |
| `ChatServiceImpl#buildExecutableToolContext` | Tool / MCP `_meta` 注入 |
| `AskDataQueryServiceImpl` | 问数强制租户过滤 |
| `ApiToolExecutionService` | HTTP 工具 Header/Query 模板 |
| [ASK_DATA.md](ASK_DATA.md) | 问数产品与技术方案 |
| [API_GUIDE.md](API_GUIDE.md) | 通用 API 指引 |
| 开放文档「在线测试」 | 可直接试 `callerContext` |

---

## 12. 最小可行示例

**目标**：同一数据集同时支持行业（按地区）与企业（按企业）隔离。

1. 数据模型字段含 `regionId`、`enterpriseId`，池中写入多样例。
2. 数据集：

```json
{
  "tenantDimensions": {
    "regionId": { "field": "regionId", "match": "subtree" },
    "enterpriseId": { "field": "enterpriseId", "match": "eq" }
  }
}
```

3. Agent 勾选该可问模型；API Key 绑定该 Agent。
4. 行业对话（不传企业）：

```json
{
  "agentId": "<agentId>",
  "message": "统计订单数量",
  "callerContext": { "regionId": "510100" }
}
```

预期：仅按成都及下级地区过滤，**不**按企业过滤。

5. 企业对话：

```json
{
  "agentId": "<agentId>",
  "message": "统计订单数量",
  "callerContext": { "enterpriseId": "ent_001", "regionId": "510100" }
}
```

预期：仅 `enterpriseId = ent_001`；**忽略** `regionId`（企业全量，不按地区收窄）。
